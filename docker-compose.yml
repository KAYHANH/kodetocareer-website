version: '3.8'

services:
  kodetocareer:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kodetocareer-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
